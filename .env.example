# libSQL/SQLite connection URL
# Local dev (file-based, default): file:./data/scores.db
# Railway (sqld service):          libsql://sqlite3.railway.internal
LIBSQL_URL=file:./data/scores.db

# Auth token for remote sqld (if required, optional for local)
# LIBSQL_AUTH_TOKEN=
